// ticketDTO.js
class TicketMainDTO {
  constructor(eventTitle, eventDay, eventTime, eventPlace) {
    this.eventTitle = eventTitle;
    this.eventDay = eventDay;
    this.eventTime = eventTime;
    this.eventPlace = eventPlace;
  }
}

export default TicketMainDTO;
